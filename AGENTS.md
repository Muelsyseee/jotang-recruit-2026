# Agent Rules

## 职责范围

- Agent 的职责仅限于本地仓库管理，例如维护目录结构、命名、索引、链接说明和仓库管理文档。
- Agent 不得实现、补全、重构或修改任何题目的实际内容，包括代码、答案、实验结果、笔记、图片及其他解题文件。
- 对已有题目文件只允许在用户明确要求时进行目录级的移动或重命名，不得改变文件内容。
- 可以新建仅用于仓库管理的文件，如 `README.md`、`AGENTS.md` 和 `.gitkeep`。

## 远程仓库限制

- 只管理当前本地仓库，不得访问或更改 GitHub 及任何其他远程仓库。
- 不得执行 `git fetch`、`git pull`、`git push`、`git clone` 或调用 GitHub API、GitHub CLI。
- 本地 Git 操作应保持非破坏性，且仅用于检查与管理当前仓库。
